name: SmartHealth
options:
    deploymentTarget:
        iOS: 15.0
targets:
    SmartHealth:
        type: application
        platform: iOS
        sources: [App, Models, Services, Views]
        settings:
            PRODUCT_BUNDLE_IDENTIFIER: com.salvatore.smarthealthdev
            INFOPLIST_FILE: SmartHealth/Info.plist
            IPHONEOS_DEPLOYMENT_TARGET: "15.0"
info:
    path: SmartHealth/Info.plist
